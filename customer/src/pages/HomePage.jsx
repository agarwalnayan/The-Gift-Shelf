import HomepageRenderer from '../components/home/framework/HomepageRenderer.jsx';

/**
 * HomePage
 * Now powered by the new Homepage Framework
 * All rendering logic is handled by HomepageRenderer
 */
const HomePage = () => {
  return <HomepageRenderer />;
};

export default HomePage;
